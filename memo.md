
### 1. 雙模輸入系統 (Dual-Mode System)

這套程序支持兩種完全不同的使用場景：

* **交互模式 (Interactive Mode)：** 直接運行 `python3 ime.py`。提供類似搜狗/微軟拼音的實時反饋介面，支持連打、選詞、翻頁。
* **快捷模式 (Quick Convert)：** 通過參數運行，例如 `ime nps -a`。
* `-a[N]`：列出該拼音的所有候選詞（可指定數量）。
* `-r[N]`：隨機組合 N 組結果。
* `-o`：轉換後自動打開瀏覽器搜索結果。



---

### 2. 智能記憶與自動造詞 (Smart Learning)

這是程序的核心靈魂，實現了「越用越好用」：

* **動態詞頻排序：** 字典加載後，會根據 `user_history.json` 的記錄，將你最常用的詞排在候選框的首位。
* **Copy-to-Learn (核心邏輯)：** 程序**退出時**會掃描所有即將複製到剪貼板的文字：
* 它會自動識別 2-6 字的漢字片段。
* **無感學習：** 即使你分開輸入「起」、「重」、「機」，只要最後它們連在一起，下次你輸入 `qizhongji`，系統就會優先推薦「起重機」。


* **長詞預測 (Ghost Text)：** 補全預測會優先選擇「歷史頻率最高」且「長度較長」的詞，按 `Tab` 即可採納。

---

### 3. 高級交互與翻頁 (Navigation)

為了適應不同用戶的輸入習慣，優化了終端操作：

* **靈活翻頁：**
* **下一頁：** 支持 `.` (句號) 或 `=` (等號)。
* **上一頁：** 支持 `,` (逗號) 或 `-` (減號)。


* **首選上屏：** 支持「空格鍵」直接選取第一個詞。
* **原樣上屏：** 輸入拼音後按「回車鍵」，可將原始拼音直接輸入。

---

### 4. 系統與字典集成 (Integration)

* **全自動剪貼板同步：** 無論哪種模式，轉換結果都會自動同步到系統剪貼板（pbcopy/xclip）。
* **多源字典加載：** 1.  `dict_hanzi.json` (基礎單字)
2.  `dict_cizu.json` (標準詞庫)
3.  `user_dicts/*.json` (用戶自定義字典)
4.  `user_history.json` (動態學習的權重庫)

---

### 💡 總結清單（下次忘記時的關鍵詞）：

1. **`-=` 與 `,.` 雙翻頁系統。**
2. **連打分詞與長詞優先推薦。**
3. **退出時觸發的 `learn_from_text` (Copy-to-Learn) 邏輯。**
4. **支持 `-a`, `-r`, `-o` 的命令行快捷模式。**
5. **不具備手動管理字典的功能（已按要求刪除）。**

這份清單涵蓋了目前代碼的所有精華。如果你在使用中發現某個邏輯不如預期，可以隨時針對其中一點要求我修改。下一步，你想要我為你演示如何編寫一個特定的 `user_dicts` JSON 字典嗎？
