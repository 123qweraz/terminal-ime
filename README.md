# Terminal IME

一个极简的 Linux 命令行汉字输入工具。无需图形界面，打字后自动复制到剪贴板。

## 特性
- **极简设计**：纯 Python 编写，仅依赖 `xclip`。
- **智能识别**：小写字母输入拼音，大写字母、数字和符号直接输入。
- **翻页支持**：使用 `-` / `=` 轻松翻页。
- **自动复制**：按 `Ctrl+D` 退出时，所有输入内容自动进入剪贴板。

## 安装步骤

1. **安装依赖**：
   ```bash
   sudo apt install xclip
